import { describe } from "node:test";

describe('spoonAPI', () => {
    describe('get spoonAPI route', () => {
        describe('given the data does not exist', () => {
            it('should return a 500 and error message', () => {
                expect(true).toBe(true);
            })
        })
    })
})